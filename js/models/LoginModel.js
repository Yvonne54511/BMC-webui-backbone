var LoginModel = Backbone.Model.extend({
    // Define the model's attributes and methods here
    defaults: {
        username:' ',
        password:' '
    },

    //urlRoot:'/redfish/v1/SessionService/Sessions'
});